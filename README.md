```markdown
# День 2 — Условия (if, else, else if), switch, сравнения, логические операторы

**Дата:** 13 марта 2026  
**Цель дня:** научиться принимать решения в коде, проверять условия, комбинировать несколько проверок, использовать switch

## Ключевые конструкции дня

- `if (условие) { ... } else { ... }`
- `else if` — цепочка условий
- Логические операторы: `&&` (и), `||` (или), `!` (не)
- Операторы сравнения: `==`, `!=`, `>`, `>=`, `<`, `<=`
- `switch` (для конкретных значений, особенно удобно с enum или строками в C# 8+)

## Уровень 1 — Beginner  
**Задача**  
Запросить число от пользователя и сказать — чётное оно или нечётное.

**Ожидаемый вывод (примеры):**
```
Введите целое число: 8
Число 8 — чётное

Введите целое число: 17
Число 17 — нечётное
```

**Подсказка:** остаток от деления → `% 2 == 0`

**Место для кода:**
```csharp
// ────────────────────────────────────────────────
//          УРОВЕНЬ 1 — ТВОЙ КОД
    Console.Write("Пожалуста, введите число: ");
    int number;
    while(!int.TryParse(Console.ReadLine(), out  number))
    {
        Console.Write("Пожалуйста, введите корректное число: ");
    }
    bool IsEven = number % 2 == 0;
    Console.WriteLine($"Число {number} — {(IsEven ? "четное" : "нечетное")}.");
// ────────────────────────────────────────────────

```

**Статус:** ✔ сделано

## Уровень 2 — Средний  
**Задача**  
Запросить температуру в °C (вещественное число) и выдать рекомендацию по одежде:

- < 0     → "Очень холодно! Надень пуховик, шапку, шарф"
- 0–10    → "Холодно. Тёплая куртка обязательна"
- 11–20   → "Прохладно. Лёгкая куртка или свитер"
- 21–30   → "Комфортно. Можно в футболке"
- > 30    → "Жара! Бери воду и крем от загара"

**Ожидаемый вывод (пример):**
```
Введите температуру (°C): 18
Прохладно. Лёгкая куртка или свитер
```

**Место для кода:**
```csharp
// ────────────────────────────────────────────────
//          УРОВЕНЬ 2 — ТВОЙ КОД
  Console.Write("Введите температуру (°C): ");
  double temp;
  while(!double.TryParse(Console.ReadLine(), out  temp))
  {
      Console.Write("Пожалуйста, введите корректную температуру: ");
  }
  string s=string.Empty;
  if(temp<0)
  {
      s="Очень холодно! Надень пуховик, шапку, шарф";
  }
  else if (temp<10)
  {
      s="Холодно. Тёплая куртка обязательна";
  }
  else if(temp<20)
  {
      s="Прохладно. Лёгкая куртка или свитер";
  }
  else if(temp<30)
  {
      s="Комфортно. Можно в футболке";
  }
  else
  {
      s="Жара! Бери воду и крем от загара";
  }
  if(string.IsNullOrEmpty(s))
  {
      s="Error";
  }
  Console.WriteLine(s);
// ────────────────────────────────────────────────

```

**Статус:** ✔ сделано

## Уровень 3 — Сложный  
**Задача**  
Запросить три оценки (от 0 до 5, double) → посчитать средний балл → определить итоговую оценку и статус:

| Средний балл     | Оценка     | Статус                     |
|------------------|------------|----------------------------|
| ≥ 4.5            | Отлично    | Поздравляем!               |
| 3.5 – <4.5       | Хорошо     | Неплохо, можно лучше       |
| 3.0 – <3.5       | Удовл.     | На троечку прошёл          |
| < 3.0            | Неуд.      | Надо подтянуть предмет     |

+ Добавить проверку, что каждая оценка в диапазоне 0–5  
+ Если хотя бы одна оценка < 0 или > 5 → сообщение об ошибке и не считать среднее

**Ожидаемый вывод (пример):**
```
Введите оценку 1: 4.7
Введите оценку 2: 5
Введите оценку 3: 4.2

Средний балл: 4.63 → Отлично
Поздравляем!
```

**Место для кода:**
```csharp
// ────────────────────────────────────────────────
//          УРОВЕНЬ 3 — ТВОЙ КОД
    double a=0;
    const int total=5;
    double all=0;
    string otsenki=string.Empty;
        string status=string.Empty;
    while(a<total)
    {
        
        Console.Write($"Введите оценку {a + 1}: ");
        a++;
        int ocenki;
    
        while(!int.TryParse(Console.ReadLine(), out  ocenki)||ocenki<0||ocenki>5)
        {
            Console.Write("Пожалуйста, введите корректное число: ");
        }
        all+=ocenki;
    }
    all/=(double)total;
    a=0;
    Console.Write($"Средняя оценка: {all}");
    all=Math.Round(all,MidpointRounding.AwayFromZero);
    otsenki=all switch
    {
        5=>"Отлично",
        4=>"Хорошо",
        3=>"Удовлетворительно",
        2=>"Неудовлетворительно",
        _=>"Неудовлетворительно"
    };
    Console.WriteLine($" → {otsenki}");
    status=otsenki switch
    {
        "Отлично"=>"Поздравляем!",
        "Хорошо"=>"Неплохо, можно лучше",
        "Удовлетворительно"=>"На троечку прошёл",
        "Неудовлетворительно"=>"Надо подтянуть предмет",
        _=>"Error"
    };
    Console.WriteLine(status);
// ────────────────────────────────────────────────

```

**Статус:** ✔ сделано

## Уровень 4 — Хардкор  
**Задача**  
Мини-игра «Угадай число» с подсказками и ограниченным количеством попыток

**Правила:**
- Программа загадывает число от 1 до 100 (используй `Random`)
- У игрока 7 попыток
- После каждого ввода:
  - "Слишком мало" / "Слишком много" / "Угадал!"
  - Показывать оставшиеся попытки
- Если угадал → поздравление + сколько попыток потратил
- Если попытки кончились → показать загаданное число
- Добавить уровни сложности (лёгкий/средний/сложный) → запрашивать в начале:
  - Лёгкий → 1–50, 10 попыток
  - Средний → 1–100, 7 попыток
  - Сложный → 1–1000, 5 попыток

**Пример игрового процесса:**
```
Выберите сложность (1=лёгкий, 2=средний, 3=сложный): 2
Я загадал число от 1 до 100. У тебя 7 попыток.

Попытка 1/7: 50
Слишком много! Осталось 6 попыток.

Попытка 2/7: 25
Слишком мало! Осталось 5 попыток.
...
Попытка 6/7: 42
Угадал! Число было 42. Поздравляю! Потрачено попыток: 6
```

**Место для кода:**
```csharp
// ────────────────────────────────────────────────
//          УРОВЕНЬ 4 — ТВОЙ КОД
Random random = new Random();
int popitki=0;
Console.WriteLine("Добро пожаловать в игру «Угадай число»!");
Console.WriteLine("Выберите уровень сложности: ");
Console.WriteLine("1. Лёгкий (10 попыток)");
Console.WriteLine("2. Средний (7 попыток)");
Console.WriteLine("3. Сложный (5 попыток)");
int choice;
Console.Write("Введите уровень сложности: ");
string level=Console.ReadLine();
level=level.Trim().ToLower();
choice=level switch
{
    "1" or "легкий" or "легко" => 10,
    "2" or "средний" or "средне" => 7,
    "3" or "сложный" or "сложно" => 5,
    _ => -1
};

if (choice == -1)
{
    Console.WriteLine("Некорректный уровень сложности.");
    return;
}
int life=choice;
int randomNumber = random.Next(1, 101);
while(popitki<life)
{
    Console.Write("Угадайте число от 1 до 100: ");
    int userGuess;
    while(!int.TryParse(Console.ReadLine(), out  userGuess)||userGuess<1||userGuess>100)
    {
        Console.Write("Пожалуйста, введите корректное число от 1 до 100: ");
    }
    popitki++;
    if(userGuess==randomNumber)
    {
        Console.WriteLine($"Поздравляем! Вы угадали число {randomNumber} за {popitki} попыток.");
        break;
    }
    else if(userGuess<randomNumber)
    {
        Console.WriteLine($"Слишком маленькое число. Попробуйте снова. Осталось попыток: " + (life - popitki));
    }
    else
    {
        Console.WriteLine($"Слишком большое число. Попробуйте снова. Осталось попыток: " + (life - popitki));
    }
    if(popitki==life)
    {
        Console.WriteLine($"К сожалению, вы исчерпали все попытки. Загаданное число было: {randomNumber}");
        break;
    }
}
// ────────────────────────────────────────────────

```

**Статус:** ✔ сделано

## План на сегодня (3–4 часа)
1. Уровень 1 → 20–40 мин  
2. Уровень 2 → 40–60 мин  
3. Уровень 3 → 50–90 мин  
4. Уровень 4 → самый интересный, но и самый объёмный — 60–150 мин

**Совет:**  
Начинай с уровня 1 → каждый уровень можно делать в отдельном проекте или в одном файле с комментариями-разделителями.  
Уровень 4 — хороший кандидат, чтобы начать использовать методы (отдельный метод для проверки ввода, для вывода подсказки и т.д.)

Когда сделаешь хотя бы один уровень — присылай код, проверю и подскажу улучшения.  

```

Удачи сегодня!  
